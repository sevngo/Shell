#include "libft.h"

/*
**	-------------------------------------------------------------------------- +
**	man 3 getch
**	-------------------------------------------------------------------------- +
*/

int	ft_getch(void)
{
	char	ch;
	int		ret;

	if ((ret = read(STDIN_FILENO, &ch, 1)) == -1)
		return (-1);
	return (ch);
}
